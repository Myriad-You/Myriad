# v0.5.3 compatibility fixture

`docker-compose.yml` is copied unchanged from the published `v0.5.3` tag. It exercises the new updater against the previous release’s deployment file, including upgrade and rollback image selection. Do not refresh it from the current Compose template.
