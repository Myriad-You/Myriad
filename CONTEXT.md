# Myriad

Myriad 是个人数字生活平台，落到产品上是自托管的个人主页与创作工坊。前者是品类，对外主句用后者。本文件是全仓库的术语表：只定义本项目特有的概念，不记录实现细节，也不作为规格文档。

## Language

### TAPP 授权链

同一个「权限」在授权链上有三种形态，任何关于「放宽权限」的讨论都必须先说清指哪一层。

**声明权限（declared permission）**：
TAPP 在 manifest 中列出的、它希望获得的权限。只是请求，不代表可用。
_Avoid_: manifest 权限

**批准权限（approved permission）**：
安装时落库的、用户同意授予该次安装的权限子集，是声明权限的子集。
_Avoid_: 已安装权限、安装权限

**授予权限（granted permission）**：
运行时按当前用户角色与平台配置过滤后，TAPP 实际可用的权限。授权链上唯一决定行为的一层。
_Avoid_: 有效权限、实际权限、运行时权限

**下放（delegation）**：
管理员把一项 elevated 权限开放给普通用户或访客的动作。下放改变的是授予权限，与声明和批准无关。
_Avoid_: 开放、放开、授权

**默认下放（default delegation）**：
某项权限在平台未经任何管理员配置时是否已被下放。这是产品决策，与「库里是否落过该配置」是两件事。
_Avoid_: 默认权限、默认开启

### TAPP 凭据

**应用凭据（installation credential）**：
某次 TAPP 安装自行配置的密钥。只写——配置后无法读回，加密存放，绑定到 manifest 声明的固定 origin。
_Avoid_: TAPP secret、用户凭据、per-tapp 凭据

**宿主密钥（host secret）**：
平台自身配置的密钥。可被 TAPP 引用用于出站请求，但永不进入沙箱，也不出现在任何返回给 TAPP 的载荷里。
_Avoid_: 全局 secret、共享凭据、平台 token

**只写（write-only）**：
密钥可被写入和使用，但任何读取路径（API、UI、模板上下文、沙箱）都只能得到「是否已配置」，拿不到值本身。
_Avoid_: 加密存储（加密是手段，只写是性质）

**动态签名（dynamic signing）**：
宿主用凭据对出站请求内容做运算后注入签名，区别于把凭据原样塞进固定请求头的静态注入。
_Avoid_: 请求鉴权、HMAC 注入

### TAPP 包结构

**层（layer）**：
包内代码按运行形态划分的单位——共享层、页面层，以及每个小组件各自一层。层由 manifest
声明，不是从一个文件里按注释切出来的段。
_Avoid_: 模式、代码段

**共享层（core）**：
三种运行形态都先执行的那一层，也是后台常驻唯一执行的代码。声明后台常驻能力的应用必须
有它。这里的「共享」指跨运行形态共享代码，与安装共享数据无关。
_Avoid_: 主入口、main、共享数据（那是存储，不是代码层）

**层入口（layer entry）**：
manifest 为某一层声明的那个 `.js` 文件。层内其余文件由入口引用进来，不需要逐个声明；
安装校验只认声明的入口，不按文件名猜测回退。
_Avoid_: 主文件、main.js

**层闭包（layer closure）**：
从层入口出发、经静态 `require` 走到的那一组模块。下发与隔离按闭包算，不按 `page/`、
`widget/` 目录名。入口可以放在任意安全包内路径。
_Avoid_: 层目录、共享文件（那是旧的目录归属说法）

**作者样式**：
由层自己声明的 CSS。与宿主预编译产物是两条并行通道——后者由平台按固定路径写入，不参与
声明校验，也不能顶替作者声明的层样式。
_Avoid_: 统一/分离模式、cssMode

### TAPP 沙箱生命周期

**常驻（resident）**：
应用声明在不可见时仍需保持运行的资格。由 App 级的 `TappBackgroundRunner` 以 headless 方式跨页承接——没有 iframe，只有后台 core。是否真的继续运行，由暂存池按常驻资格分档决定。与「隐藏」的区别：隐藏保留页面内 iframe，常驻是跨页的 headless 存活。
_Avoid_: 后台常驻、保活、后台运行

**可见出口（visibility escape hatch）**：
让用户看到当前后台存活的常驻实例、并单独停掉它们的界面入口。给「看不见却活着」一个可见、可控制的口子。
_Avoid_: 后台管理、任务管理器

**隐藏（hidden）**：
沙箱保持存活但用户看不见：iframe 留在原地，桥、授予权限与会话令牌全部保留，只被通知停止活动（`lifecycle:pause`）。滚出视野、切 Tab、窗口最小化走这条路径。隐藏是页面内的概念——切换页面会卸载页面内沙箱（销毁），跨页状态由常驻承接。不再区分「暂停」与「暂存」——iframe 被移到另一个父节点会被浏览器强制重新加载，「移出视图但存活」在实现上只能是「留在原地藏起来」。
_Avoid_: 暂停、暂存、冻结、挂起、缓存、休眠

**暂存池（stash pool）**：
持有隐藏实例的组件，负责容量、淘汰与常驻名额准入。它是隐藏实例的容器，不是独立于隐藏的第四种状态。
_Avoid_: 缓存池、实例缓存

**销毁（destroy）**：
沙箱及其全部资源被释放，不可恢复：iframe、桥、授予权限、会话令牌、沙箱内存状态全部释放。只在授权前提消失时发生——卸载、代码变更、登录态变更、权限变更、恢复时发现前提已消失、暂存池淘汰。销毁后应用仍装着，下次可重新创建。
_Avoid_: 关闭、清理、DOM 卸载

**卸载（uninstall）**：
用户把 TAPP 从平台移除。先销毁该应用的全部实例（运行的、隐藏的、后台存活的），再清除存储与注册资源，顺序不可颠倒。卸载后应用不再装着，需重新安装；销毁后应用仍装着。属于安装层，销毁属于实例层。
_Avoid_: 删除、移除、实例卸载

### TAPP 能力面

**能力位图（capability bitmap）**：
以权限枚举为单位的授权模型——授予一项权限即开放该权限对应的全部方法。
_Avoid_: 权限模型（太笼统）

**端点目录（endpoint catalog）**：
以单个端点为单位的授权模型——TAPP 在 manifest 中声明要调用哪些端点，平台逐端点鉴权、限流、审计。
_Avoid_: API 网关（指的是承载它的组件，不是模型本身）

**出站端点（outbound endpoint）**：
TAPP 声明、由平台代为发起的对外请求。
_Avoid_: http API、代理请求

**入站端点（inbound endpoint）**：
TAPP 声明、供平台外部或其他 TAPP 调用的接口。使 TAPP 从被动的插件变成可被调用的服务。
_Avoid_: webhook（webhook 只是入站端点的一种调用方）

### TAPP 规则边界

**契约（contract）**：
跨 CLI、前端、后端共享的静态事实：权限目录、路径与存储键规则、Manifest 校验、宿主 CSS 固定路径。不含授予，也不含 HMAC 或 pipeline 求值。
_Avoid_: 把授予、HMAC、transform 求值写进契约

**规则（rules）**：
无 I/O 的求值：HMAC、transform pipeline、已安装资源计划、Agent 文本上限与出站错误归类。backend 只做 re-export，不在规则层做授权判定。
_Avoid_: 把规则接到数据库、Web 框架或 HTTP 客户端

**I/O 墙（I/O wall）**：
契约与规则不得依赖数据库、Web 框架或 HTTP 客户端。出站、落库、Axum 映射留在 backend。
_Avoid_: 用 HTTP 客户端做 URL 校验、把纯校验放进 API 层

### TAPP 存储

**主体私有存储（subject storage）**：
`Tapp.storage` 按当前运行主体隔离。打开公开安装时，每个用户或签名游客读写自己的空间，读不到站长数据。
_Avoid_: 用户存储、本地存储

**安装设置（installation settings）**：
Manifest 声明过的配置项。落在安装 owner 命名空间，owner / 管理员可写，能打开该安装的人可读。只放开关、主题、刷新间隔这类配置。
_Avoid_: 全局配置、共享 storage

**安装共享数据（installation shared data）**：
安装级 KV，语义是数据仓库而不是配置。同样落在安装 owner 命名空间：owner / 管理员写入，访客读同一份。公开部署用来展示站长数据。
_Avoid_: 共享 storage、全局 storage、站点仓库、Tapp.data（那是数据处理 API）、共享层（那是代码层，不是存储）

三种 KV 的写入成功只表示落盘。跨沙箱同步靠各自的 `onChanged`；重跑 Widget `render()`
靠显式 `invalidate`，或 storage / shared 仍保留的兼容 remount。不要把落盘当成刷新指令。

### 首页布局

**标准布局**：首页小组件落在居中、有最大宽度的宫格里，桌面 16×4，窄屏按列档紧凑重排。

**自由布局**：格子像素大小与当前视口下的标准布局相同，画布固定 16 列 × 8 行、与标准同宽居中。两套布局各自保存坐标。
_Avoid_: 全屏网格、自适应网格（那是窄屏紧凑重排）

**教程覆层（tour overlay）**：压在当前页上的分步高亮教学。按页注册、手动开始。首页浏览和首页编辑模式是两套教程，进编辑不会自动开讲。资料库列表与无限画布是两套教程，画布套讲平移与缩放，不讲下拉加载；收藏步高亮整块画布，卡片步只钉一张卡。商店页不注册教程。设置页的搜索/侧栏/正文是一套；AI 设置里的人设卡片、以及打开后的人物设定工作台，各另有一套，与设置总览分开。人设生成向导不做教程。用户可见文案称「导览」。步骤只讲当前高亮控件上、当前身份能用的操作。实操步未完成动作不能下一步；首页浏览的 Agent 步是第一处，须长按空白处呼出面板后再继续。实操步聚焦操作区并循环演示手势。首页浏览先讲收缩态控制岛，再到该步才展开控制面板；控制面板步的高亮贴齐展开终态外壳，回到控制岛步贴齐收缩终态，都不跟 morph 中间帧。不是系统安装向导，也不是人设引导。
_Avoid_: onboarding、wizard、把人设 OnboardingWizard 当现场教程

**智能岛（control island）**：右上角收缩态控制条上的轮播。站长在模块设置里选择问候、天气、一言、音乐和 Tapp 是否出现。通知进岛走通知偏好，不走这一组。
_Avoid_: 导航岛、把信息岛当主导航、把展开面板的小组件宫格当成岛上的槽位

### Brew

**板块（board）**：
Brew 的三个顶层分区：订阅、手记、站点。板块划分的是「这批内容长什么样」，
按 `source_type` 分，不按分类分 —— 一个源挂着哪些分类不决定它去哪个板块。
收藏不是板块，它是登录态的筛选，入口在控制岛上。
_Avoid_: 分类、二级导航项、tab

**入口型来源（site source）**：
只提供一个外站入口的来源，不抓取、无未读、不进阅读器。站点板块收的就是它。
磁贴上叫 icon 构图，默认横条。
_Avoid_: 友链（那是一个分类名，不是来源类型）、书签

**手记（note）**：
站长自己写的 Markdown 内容。它不是第二套文章系统 —— 就是 `brew_items` 里的
一条，挂在一个 `source_type = note` 的本地源下面，因此阅读器、评论、注释、
播客、收藏、SEO、联邦全部照常生效。与抓来的文章只差原文列和平台生成的 guid。
_Avoid_: 博客、文章（太笼统）、草稿（那是还没发布的状态）

**渲染后正文**：
`brew_items.content` 里那份消毒过的 HTML。全站唯一被读取的正文 —— 阅读器、
RSS、联邦、SEO 摘要都读它。Markdown 原文只用于取回编辑器，永不在读路径上
重新渲染。
_Avoid_: 富文本、正文（不区分原文与渲染结果）

**档位锁（size lock）**：
`brew_sources.card_size` 现在的语义。有值表示用户手动挑过磁贴尺寸，派生规则
让位于它。可选档位按来源类型不同：入口型来源多一个横条档。
_Avoid_: 卡片尺寸（那是老网格的说法）

### Agent

用户界面叫 **Agent**。内部项目名是 Arael。站点助手：计划、执行、记忆、MCP。
_Avoid_: Arael Agent、Arael 智能体、把 Arael 当产品名

**办事（Work）**：
面板里计划、执行、确认的那条路径。保留现有 Agent / Pro 能力。开放给所有能用 Agent 的登录用户，能做到哪一步由授予权限逐条决定，不按身份分档。
_Avoid_: 任务模式、Agent 模式、把 PlannerStatus 的 Chat 当成产品聊天档、把办事整条按管理员开关

**聊天（Chat）**：
面板里只用人设说话的那条路径，固定走 Lite。不能拿办事工具（搜、订、生成、计划），也不能回落到 Standard / Pro。现场可以执行她决定的换装，以及当前播放器的播、停、上一首、下一首。
_Avoid_: 闲聊、planner Chat、把聊天当办事的降级分支、把搜歌换歌单放进聊天档

**提案（work proposal）**：
她注意到一件事之后，请说话对象决定要不要交给办事。接受后重新进入 Work 的计划与确认；提出提案本身不是执行授权。
_Avoid_: 自动任务、自主执行、把意图账本当成第三种面板模式

**用户接受**：
说话对象在提案卡上点头。之后办事只受当前授予权限约束，不再复验自治授权。
_Avoid_: 当成自治接受

**自治接受**：
说话对象事先给过自治授权，提案可以跳过提案卡进入办事。办事时仍要按自治授权与当前授予权限的交集执行；撤销后不能再走这条路。
_Avoid_: 第三个面板模式、Heartbeat、SYSTEM_USER_ID

**配对（pairing）**：
外部聊天账号（如 QQ openid、Telegram `from.id`、Discord 用户 snowflake、飞书 `open_id`）绑到已登录的站点用户。一次性配对码证明「这个号是我」，不是该平台的登录 OAuth，也不新开登录方式。Discord 登录身份已经占用 `discord` 这条身份名，通道配对用 `discord_dm`；飞书通道配对用 `feishu`。
_Avoid_: QQ 登录、Telegram 登录、Discord 登录、飞书登录、把配对当会话、把 openid 漏进计划器

**通道（channel）**：
外部聊天软件走进站点已经存在的办事流水线。只负责进出，不另造一个 bot 大脑。第一版只做私聊。
_Avoid_: 外部会话、IM 网关（那是实现）、把通道当第二种 Agent

**待答（pending prompt）**：
办事停在澄清、确认或执行中提问时，通道上挂起的那一个问题。下一条回复或按钮只消费这一问。
_Avoid_: 把按钮当全局是/否、把旧问题的回答套到新任务上

**投递（delivery）**：
把终态或待答送回原来那条私聊。失败只补发这一份，不重新执行。
_Avoid_: 发送失败就重跑任务、截断后丢掉后半段

### Merope

内部项目名是 Merope。用户界面叫 **Agent 人设**（en: Agent persona，ja: Agent ペルソナ）。站点一份的说话人格与上半身形象。这和 Myriad 作为「个人主页与创作工坊」的定位不是一回事。
_Avoid_: Agent 生命、Agent life、Digital Life、数字生命、数字伙伴、companion（产品名）、人設（日语职衔，不是 人设）

**设定（persona）**：
全站一份的名字、性格和立绘，只有站长能写。人设关掉时对外叫 Agent。人设开着但没写名字时对外叫 Arael——那是人设默认名，不是产品名。对谁说话都是这同一个人。
_Avoid_: 档案、人设文件、SOUL

**名片（persona card）**：
TAPP 只读投影：公开名字、人设是否开启、心情带、正在做什么、同源主立绘路径。不是性格正文，不是心情数字，也不是现场 Rig。
_Avoid_: 感知、表演、开口、把立绘字节塞进桥、给 TAPP 写人设

**视觉设定（visual profile）**：
从已确认 persona 派生并由站长确认的外观契约。人模块（脸、眼睛、头发）全站一份；衣服模块（上身服装、材质、配色、饰品和袖/局部手臂）按衣柜套装各存一份。它决定立绘生成，但不进入说话人格。改人模块会让旧立绘与骨骼失效；改一套衣服只让那一套的立绘与骨骼失效。做新衣服或改未穿着的那套，不改正在穿着的视觉设定，除非换上。
_Avoid_: 性格、立绘提示词、全身服装档案、把人模块和衣服模块写成一份活文档、做新衣服就换上

**形象（face）**：
页面上同时只播放一份上半身 2.5D，由主立绘或分层 PSD 编译而来。立绘和骨骼按衣柜套装保存；换装在正在播放的那一份上实时切换。没有肩—肘—腕—手的关节链；左右袖口/局部前臂/手可以是独立刚性整层，相对静止位只允许在 ±15° 内摆动。
_Avoid_: 伙伴、companion、挂件、换装就删掉其它套的骨骼、换装就刷新现场播放器

**贴纸头像（sticker avatar）**：
从已确认主立绘派生的一张 Q 版方形贴纸，站点一份，只有站长能生成。画面只有头（含头发与头饰），不出肩、颈、身子。它是静止图像，不是形象——不进现场播放器，也不参与动作。用在头像位与 Agent 通知图标：任何登录用户都可以把它选成自己的画像源。血统锚在主立绘上，换主立绘就作废，需要重做。不按衣柜套装存，换装不影响它。
_Avoid_: 小头像、Q 版立绘、胸像、半身、带身子的 Q 版小人、把它当第二份形象、按套装各存一张

**形象在场（face presence）**：
形象出现、离开、以及小组件和面板交还播放权时共用的一层过渡。不是换衣服的骨骼表演，也不是第二套现场播放器。出现和离开走退场再入场；交还播放权也走离开再出现。退场比入场短。退场时留下最后一帧平面影像，卸掉现场播放器。换装是同一份现场播放器上换包装，不退场、不卸播放器。现场播放器仍只有一份。
_Avoid_: 换装关节动画、两套现场同时播、用主立绘占位做过渡、新旧交叉叠化、把换装做成退场刷新

**动作通道（motion channel）**：
形象上嘴、表情、视线、头身各自独立的控制权。说话只占嘴，唱歌律动仍可留在头身；显式表演只压低它占用的那几路。头发和衣物物理是有界叠加，不是互斥开关。
_Avoid_: 把唱歌和说话当成全身互斥状态、把音乐直写 Rig 当成第三条旁路、把布尔主人当成姿势开关、把唱歌和闲置当成互斥模式

**动作租约（motion lease）**：
某一来源对若干动作通道的瞬时控制权，有独立句柄和期限。卸下一个形象只释放它自己的租约；到期后控制权还给下一优先来源。不是库存状态。
_Avoid_: 按来源单例互斥、把租约持久化成表

**动作意图（motion intent）**：
来源向协调器发布的语义动作（说话、表演、唱歌、心情）。现场形象只消费快照，不接受来源直写。有表达意义的自主反应也必须作为意图接入；呼吸、眨眼和无语义微姿态属于形象的现场基线。
_Avoid_: 把 Player 内部算法当成第三套来源、把工作台预览写进生产协调器

**行为（behavior）**：
动作意图被现场调度后的一次可执行反应，带表达功能、运动形态、身体资源和生命周期。一次性动作、持续姿态、节律同步、目标追踪和次级运动是不同形态；行为描述为什么和何时动，不包含骨骼或 driver。
调度器只管有语义的动作。闲置漂移、随机小动作和呼吸跑在形象自己的时钟上，只按权重参与合成，不进调度器、也不出现在报给导演的在场行为里——「一个行为时钟」说的是语义动作，不是全部动作。表达功能的词表因此按可产出的口径收敛：能被某个生产者发出的名字才留在契约里。
_Avoid_: 把 cue 名当行为本身、把所有运动都塞成固定时长动画、把行为层当第二套渲染器、把闲置自运动说成已经纳入统一调度、往契约里写没有生产者的表达功能

**动作单元（motion unit）**：
行为被身体实现后的唯一形态：一个形态名、一份强度与质地、一段已解析的生命周期。所有族（伴随言语、音乐、表演）实现成同一种单元；身体按族分派，不按输出形状分派。整套在场单元每次全量重述，所以重复下发不会叠加，掉出计划的行为会自行释放——调用方因此不需要记录自己播过什么。
_Avoid_: 让某一族实现回来源的线格式、把已解析的时点重新压回时长再解算、在调用侧维护"播过哪些"的账

**身体资源（behavior resource）**：
行为语义上要使用的脸、视线、头、躯干、左右肢体或次级物理区域。点分名称形成层级，父资源和子资源冲突，左右肢体可以并行。不是骨骼名，也不是渲染层名。
生产互斥用的通道是这套层级向立绘实际能驱动的部位的投影：这具骨架没有肩肘腕和腿脚，头和躯干又共用一份上半身姿势，所以 `headBody` 是当前的粒度上限，不是欠下的债。层级本身仍按未来骨架写。
_Avoid_: 用 `headBody` 永久代替身体层级、把资源名直接当 driver、把投影当成迟早要还的兼容层

**承载姿态（bearing）**：
角色在瞬时行为之下持续保持的表情底、身体开合、运动能量和注意力。它会随回合修订，但不是一段动画，也不占瞬时动作租约；停止一个 gesture 不能清掉 bearing。
_Avoid_: 给 baseline 伪造结束时间、把停止动作等同于恢复默认人格

**时间锚（time peg）**：
行为对现场事件的可变语义时点，例如语音重音、节拍或轮次结束。准备阶段可以随新证据有限调整，动作峰值一旦承诺就不再跳时；多个行为可以共享同一个时间锚。
_Avoid_: 固定延时、逐帧时间戳、把后端给的毫秒数当不可调整的播放命令

**动作生命周期（behavior lifecycle）**：
一次行为从起势到收势的七个语义时点：开始、就位、发力起、峰值、发力止、放松、结束。放松和结束可以缺省，表示行为在被替换或释放前一直持续。准备段可以随新证据改时，峰值一旦承诺就不再跳；身体执行的依据是调度器解析后的时点，不是来源给出的原始时点。
_Avoid_: 把它当固定分段动画、让每个来源各自维护自己的生命周期、把生产者原始时间直接下发给身体

**关节包络（joint envelope）**：
把合成后的姿势收进立绘资产安全范围的那一步。超出的能量转移到别的自由度，而不是整体压幅——高领挡住俯仰时，改用偏转、侧倾、视线、躯干和袖口表达同一份力度。这是空间约束，和动作生命周期的时间分段没有关系。
_Avoid_: 把它当第二套时间包络、用全局阻尼代替能量转移、把它做成渲染层特效

**占用（occupancy）**：
由现场情境导出的 0–1 通道权重，在形象 tick 上缓动。离散主人变化只改目标占用；权重倾斜，不清零仍在场的来源。不是租约，不是库存。
_Avoid_: 把占用做成租约字段、把布尔主人当成占用

**姿势合成（pose composite）**：
把各来源偏移按占用叠到休息姿势上的那一步。不是第三套来源。
_Avoid_: 把 Player 内部算法当成协调器来源

**状态摘要（rig state summary）**：
给 Strict-Lite 动作导演看的语义现场：表情、姿态、正在做的动作、通道所有者、是否在说话/唱歌、节拍档位、能力和人设动作习惯。只在事件发生时更新。不含骨骼、坐标、角度、原始 driver、逐帧数据或像素。
_Avoid_: 把 Player 快照当导演输入、按帧上报动作状态

**代数（generation）**：
同一输出来源被新请求替换时的内存计数。复用现有 runId，不另造 turnId，也不落库。新的聊天输入可以替换旧聊天的文本、语音和动作；不能取消后台办事。
_Avoid_: turnId、把代数写进数据库、把 SSE 断线当成取消任务

**句段（speech segment）**：
流式回复里一句已经稳定、可以拿去合成的话。合成可以并行，播放必须按序号。
_Avoid_: 把整段 WAV 当唯一语音单位、把 token 直接送去朗读

**语音管线（speech pipeline）**：
句段合成、有序播放、嘴通道和本地 VAD 共用的控制面。真音频驱动嘴；失败就退回文字口型，不挡回复。连续听必须人手打开。人设开口朗读也必须人手打开；关上仍走文字口型。
_Avoid_: 默认常驻录音、默认开口朗读、把关上朗读当成关掉嘴形、把 ASR 帧塞进 run hub、插话就取消办事

**感知摘要（perception snapshot）**：
按来源登记的有界观察：有版本、有过期、可替换。发给 Lite 的只是摘要，不是屏幕像素或音频帧。
_Avoid_: 把原始指针/频谱/画面不断塞进 prompt、把页面正文当系统指令

**上下文注册表（perception registry）**：
这些摘要的内存目录。同一来源替换而不是无限追加；过期即消失；默认不落库。
_Avoid_: 无控制堆 prompt、把注册表做成第三种记忆表

**状态（addressee state）**：
跟当前说话对象相处时的心情、正在做什么、是否勿扰。心情是效价×唤醒，沉默时回归人设基线。不是人格本身，也不是全站共用一个心情。
_Avoid_: 需求条、六维属性

**心情（mood）**：
跟当前说话对象相处时的中期效价×唤醒。沉默时向人设基线回归。数字不进开口模型。
_Avoid_: 把心情当成瞬时表情、把效价唤醒数字写进 prompt

**情绪（emotion）**：
更短的一层，评完当下这件事会拉一把心情。不对外展示。
_Avoid_: 把情绪当成心情、把情绪数字给前端

**说话对象（addressee）**：
这一轮对面的人，看登录对象。每一次开口都要先认清是谁。
_Avoid_: 会话、角色、访客身份（太含糊）

**人设记忆（persona memory）**：
按说话对象保存、由她决定留下的短事实。下一轮开口和意识决策会读到。不是办事记忆，不是设定正文，也不是把事件流水整段当记忆。
_Avoid_: AgentMemory、把日记时间序当记忆、把执行教训灌进闲聊

**主动对话（proactive transcript）**：
它自己找上门说的话，按说话对象独立存放。用户看不见、打不开。人在网页上可以当面说；正看着 Agent 面板时不显示、不推送 Agent 通知，没在看面板就要通知。在场和面板是两件独立的事。
_Avoid_: 会话、隐藏会话、用一个开关代替在场和面板

**事件开口（event speech）**：
名单上的事发生之后，再决定要想、要不要通知。不是到点就想。
_Avoid_: 心跳、定时主动

**现场（live presence）**：
意识层看到的瞬时观察：是否在说话、形象是否可见、当前动作/语音意图、最近感知、最近有效状态摘要。不是授予权限，也不能据此选工具或办事。
_Avoid_: 把现场当授权、把观察当执行权

**当前关注（attention segment）**：
跨多次事件活着的这一段，只延续或换段。不是记忆，不是心情标量，不进人设记忆。
_Avoid_: 会话状态、上下文窗口

**开口意图（speak intent）**：
意识层产出的、有寿命的开口件。兑现成句由另一层做。
_Avoid_: 待发消息、开口队列（那是容器不是件）

**现场入站（presence inbound）**：
和命名事件并列的第二条入站，只写现场，不触发决策。页面可见时续租观察，过期即不在。从过期回到在场可以变成一条命名问候事件——那是事件开口，不是现场入站在决策。
_Avoid_: 把观察租约当成意识心跳、上报接口、把观察当执行权

**埋点（product analytics）**：
发往站点统计的产品事件，去向是分析库，同意面是站点统计开关。与感知摘要共用「用户做了什么」这个事实，但不共用载荷、不共用同意，两条管道不得互相分流。
_Avoid_: 把埋点当感知源、把分析同意当人设可见性同意

**身体（body）**：
当前这份形象能做什么、她想表达什么、当前正在做什么。适配器把语义意图翻译成该渲染器的动作；感知适配器把本地输入变成语义摘要。现在只有 Anime2.5D。
_Avoid_: 插件系统、把 driver 交给 Lite、为尚未存在的身体预留接口

**衣柜（wardrobe）**：
同一角色下可切换的多套上身服装。每一套就是一张主立绘（同一张脸与发型，衣服不同），可以各存一份分层骨骼。点开一套只打开管理页，不换上；换上是套装页里的切换。人模块不变，衣服模块替换。换上时激活那一套的立绘和骨骼，其它套的包留在库里。不是引导流程的一页。主立绘落成的那一套是默认服装，不能改名也不能删除，也不占用衣服风格名。
_Avoid_: 把换装当重新做人、把衣服画成衣架上的样板角色、把默认服装当成可丢掉的库存、全站只准存一份骨骼

**临时换装（outfit overlay）**：
聊天档里换不换、换哪一套由 Lite 决定。现场只执行衣柜里已有的称呼，改这一轮对面看到的形象，以及她自己知道这一轮穿着哪套。不改正在穿着，不写视觉设定。新开聊天会话或她决定换回来，回到正在穿着的那一套。办事档和首页小组件仍看正在穿着。能打开 Agent 面板的人都可以这样聊，不是站长专属，也不在聊天里放换装按钮。
_Avoid_: 把聊天换装当衣柜换上、给聊天档加换装按钮、让访客写人设、用对方原话规则盖过 Lite 的决定

**回合痕迹（turn trace）**：
一次开口从输入到说完的本地 span 与延迟采样。环形缓冲，结构化日志。不进 run hub，也不记录音量帧或每个口型。
_Avoid_: telemetry 表、把播放细节持久化成事件

**首页布局模式（home layout mode）**：
站点一份的首页排法，标准或自由。不是每人一份，也不是浏览器本地开关。
_Avoid_: 个人布局、本地布局

**贴纸（sticker）**：
首页自由布局上的装饰格，不是小组件。不占小组件占用格数，不进标准布局，平板/手机回退时不参与装箱。
_Avoid_: 小组件、壁纸、图标
