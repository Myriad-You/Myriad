# Myriad

Myriad 是个人数字生活平台，落到产品上是自托管的个人主页与创作工坊。前者是品类，对外主句用后者。本文件是全仓库的术语表：只定义本项目特有的概念，不记录实现细节，也不作为规格文档。

## Language

### TAPP 授权链

同一个「权限」在授权链上有三种形态，任何关于「放宽权限」的讨论都必须先说清指哪一层。

**声明权限（declared permission）**：
TAPP 在 manifest 中列出的、它希望获得的权限。只是请求，不代表可用。
_Avoid_: manifest 权限

**批准权限（approved permission）**：
安装时落库的、用户同意授予该次安装的权限子集，是声明权限的子集。
_Avoid_: 已安装权限、安装权限

**授予权限（granted permission）**：
运行时按当前用户角色与平台配置过滤后，TAPP 实际可用的权限。授权链上唯一决定行为的一层。
_Avoid_: 有效权限、实际权限、运行时权限

**下放（delegation）**：
管理员把一项 elevated 权限开放给普通用户或访客的动作。下放改变的是授予权限，与声明和批准无关。
_Avoid_: 开放、放开、授权

**默认下放（default delegation）**：
某项权限在平台未经任何管理员配置时是否已被下放。这是产品决策，与「库里是否落过该配置」是两件事。
_Avoid_: 默认权限、默认开启

### TAPP 凭据

**应用凭据（installation credential）**：
某次 TAPP 安装自行配置的密钥。只写——配置后无法读回，加密存放，绑定到 manifest 声明的固定 origin。
_Avoid_: TAPP secret、用户凭据、per-tapp 凭据

**宿主密钥（host secret）**：
平台自身配置的密钥。可被 TAPP 引用用于出站请求，但永不进入沙箱，也不出现在任何返回给 TAPP 的载荷里。
_Avoid_: 全局 secret、共享凭据、平台 token

**只写（write-only）**：
密钥可被写入和使用，但任何读取路径（API、UI、模板上下文、沙箱）都只能得到「是否已配置」，拿不到值本身。
_Avoid_: 加密存储（加密是手段，只写是性质）

**动态签名（dynamic signing）**：
宿主用凭据对出站请求内容做运算后注入签名，区别于把凭据原样塞进固定请求头的静态注入。
_Avoid_: 请求鉴权、HMAC 注入

### TAPP 包结构

**层（layer）**：
包内代码按运行形态划分的单位——共享层、页面层，以及每个小组件各自一层。层由 manifest
声明，不是从一个文件里按注释切出来的段。
_Avoid_: 模式、代码段

**共享层（core）**：
三种运行形态都先执行的那一层，也是后台常驻唯一执行的代码。声明后台常驻能力的应用必须
有它。这里的「共享」指跨运行形态共享代码，与安装共享数据无关。
_Avoid_: 主入口、main、共享数据（那是存储，不是代码层）

**层入口（layer entry）**：
manifest 为某一层声明的那个 `.js` 文件。层内其余文件由入口引用进来，不需要逐个声明；
安装校验只认声明的入口，不按文件名猜测回退。
_Avoid_: 主文件、main.js

**层闭包（layer closure）**：
从层入口出发、经静态 `require` 走到的那一组模块。下发与隔离按闭包算，不按 `page/`、
`widget/` 目录名。入口可以放在任意安全包内路径。
_Avoid_: 层目录、共享文件（那是旧的目录归属说法）

**作者样式**：
由层自己声明的 CSS。与宿主预编译产物是两条并行通道——后者由平台按固定路径写入，不参与
声明校验，也不能顶替作者声明的层样式。
_Avoid_: 统一/分离模式、cssMode

### TAPP 沙箱生命周期

**常驻（resident）**：
应用声明在不可见时仍需保持运行的资格。由 App 级的 `TappBackgroundRunner` 以 headless 方式跨页承接——没有 iframe，只有后台 core。是否真的继续运行，由暂存池按常驻资格分档决定。与「隐藏」的区别：隐藏保留页面内 iframe，常驻是跨页的 headless 存活。
_Avoid_: 后台常驻、保活、后台运行

**可见出口（visibility escape hatch）**：
让用户看到当前后台存活的常驻实例、并单独停掉它们的界面入口。给「看不见却活着」一个可见、可控制的口子。
_Avoid_: 后台管理、任务管理器

**隐藏（hidden）**：
沙箱保持存活但用户看不见：iframe 留在原地，桥、授予权限与会话令牌全部保留，只被通知停止活动（`lifecycle:pause`）。滚出视野、切 Tab、窗口最小化走这条路径。隐藏是页面内的概念——切换页面会卸载页面内沙箱（销毁），跨页状态由常驻承接。不再区分「暂停」与「暂存」——iframe 被移到另一个父节点会被浏览器强制重新加载，「移出视图但存活」在实现上只能是「留在原地藏起来」。
_Avoid_: 暂停、暂存、冻结、挂起、缓存、休眠

**暂存池（stash pool）**：
持有隐藏实例的组件，负责容量、淘汰与常驻名额准入。它是隐藏实例的容器，不是独立于隐藏的第四种状态。
_Avoid_: 缓存池、实例缓存

**销毁（destroy）**：
沙箱及其全部资源被释放，不可恢复：iframe、桥、授予权限、会话令牌、沙箱内存状态全部释放。只在授权前提消失时发生——卸载、代码变更、登录态变更、权限变更、恢复时发现前提已消失、暂存池淘汰。销毁后应用仍装着，下次可重新创建。
_Avoid_: 关闭、清理、DOM 卸载

**卸载（uninstall）**：
用户把 TAPP 从平台移除。先销毁该应用的全部实例（运行的、隐藏的、后台存活的），再清除存储与注册资源，顺序不可颠倒。卸载后应用不再装着，需重新安装；销毁后应用仍装着。属于安装层，销毁属于实例层。
_Avoid_: 删除、移除、实例卸载

### TAPP 能力面

**能力位图（capability bitmap）**：
以权限枚举为单位的授权模型——授予一项权限即开放该权限对应的全部方法。
_Avoid_: 权限模型（太笼统）

**端点目录（endpoint catalog）**：
以单个端点为单位的授权模型——TAPP 在 manifest 中声明要调用哪些端点，平台逐端点鉴权、限流、审计。
_Avoid_: API 网关（指的是承载它的组件，不是模型本身）

**出站端点（outbound endpoint）**：
TAPP 声明、由平台代为发起的对外请求。
_Avoid_: http API、代理请求

**入站端点（inbound endpoint）**：
TAPP 声明、供平台外部或其他 TAPP 调用的接口。使 TAPP 从被动的插件变成可被调用的服务。
_Avoid_: webhook（webhook 只是入站端点的一种调用方）

### TAPP 规则边界

**契约（contract）**：
跨 CLI、前端、后端共享的静态事实：权限目录、路径与存储键规则、Manifest 校验、宿主 CSS 固定路径。不含授予，也不含 HMAC 或 pipeline 求值。
_Avoid_: 把授予、HMAC、transform 求值写进契约

**规则（rules）**：
无 I/O 的求值：HMAC、transform pipeline、已安装资源计划、Agent 文本上限与出站错误归类。backend 只做 re-export，不在规则层做授权判定。
_Avoid_: 把规则接到数据库、Web 框架或 HTTP 客户端

**I/O 墙（I/O wall）**：
契约与规则不得依赖数据库、Web 框架或 HTTP 客户端。出站、落库、Axum 映射留在 backend。
_Avoid_: 用 HTTP 客户端做 URL 校验、把纯校验放进 API 层

### TAPP 存储

**主体私有存储（subject storage）**：
`Tapp.storage` 按当前运行主体隔离。打开公开安装时，每个用户或签名游客读写自己的空间，读不到站长数据。
_Avoid_: 用户存储、本地存储

**安装设置（installation settings）**：
Manifest 声明过的配置项。落在安装 owner 命名空间，owner / 管理员可写，能打开该安装的人可读。只放开关、主题、刷新间隔这类配置。
_Avoid_: 全局配置、共享 storage

**安装共享数据（installation shared data）**：
安装级 KV，语义是数据仓库而不是配置。同样落在安装 owner 命名空间：owner / 管理员写入，访客读同一份。公开部署用来展示站长数据。
_Avoid_: 共享 storage、全局 storage、站点仓库、Tapp.data（那是数据处理 API）、共享层（那是代码层，不是存储）

**安装私有数据（installation-private data）**：
安装级自由 KV，落在安装 owner 命名空间，仅 owner / 管理员可读可写。游客和普通登录用户完全不可见。不是主体私有存储，也不是应用凭据——明文会进沙箱，密钥仍走只写凭据。
_Avoid_: 私有存储（那是 Tapp.storage）、Tapp.vault、把站点 token 放进可读 KV

四种 KV 的写入成功只表示落盘。跨沙箱同步靠各自的 `onChanged`；重跑 Widget `render()`
靠显式 `invalidate`，或 storage / shared 仍保留的兼容 remount。`Tapp.private` 与 settings 一样只广播，不拆卡。不要把落盘当成刷新指令。

### 站长与认领

**站长（site owner）**：
站点唯一的所有者账号，也是公开安装与站点数据所属的命名空间。站长一定是管理员，不能被
降级或删除；反过来，管理员不一定是站长。旧库里尚未标出站长时，由最早的管理员暂代。
_Avoid_: 第一个管理员、admin 用户（那是角色，不是身份）

**认领（installation claim）**：
站点已经有管理员或站长这件事。认领之后安装向导关闭。读不出是否已认领时按已认领处理——
不能确认没有主人，就不放行初始化。
_Avoid_: 有没有用户、有人登录过

### 联邦闸门

**出口地理位置（egress location）**：
按本机公网 IP 判定的服务器所在地，由启动时的一次性探测得出。问的是「这台机器在哪」，
所以探测绕开出站代理；它不是访客地理位置，也不是站点域名归属地。
_Avoid_: 客户端地区、访客 IP、站点区域

**联邦闸门（federation gate）**：
出口地理位置决定本进程能不能跑联邦的那个开关。每次启动重新判定——机器会搬家，所以它
只在内存里，不落 `configurations`，也没有管理员开关。关闭时全部 `federation:*` 授予权限
对所有角色（含管理员）为假；专用联邦进程随之退出，不空转。Web / 人设 / 本地合并进程
不退出，合并进程里联邦路径仍答 404。
_Avoid_: 联邦开关（听起来像可配置项）、把它当权限下放、把它写成平台配置字段

**失败放行（fail-open）**：
地理位置服务不可达、被限流或返回无法解析时，联邦保持开启。丢一个第三方查询不该顺手
把联邦弄停；只有明确读到「本机在受限地区」才关闸。探测未回来的那几秒同样按开启算，
但对外投递例外——它要等到判定落定才发第一批。
_Avoid_: 把探测失败当成受限地区、把未决状态当成已关闭

### 首页布局

**站点身份（site identity）**：
管理员配置的站点标题、描述与图标。爬虫和分享预览的 HTML 由 backend 写入；给人看的 SPA 文档由 frontend 写入。proxy 只转发，不改 HTML。
_Avoid_: 在 proxy 里注入标题、把 SEO 壳和 SPA 壳当成同一份文档

**宿主界面语言**：
站点 SPA 的 BCP-47 目录：`zh-CN`、`zh-TW`、`en-US`、`ja-JP`、`ko-KR`、`fr-FR`、`de-DE`。不是 Tapp 包内作者语言表，也不是仓库 README 的 en / zh / ja 三份。
_Avoid_: 只写三语、把 README 语言切换当宿主 locale 全集

**标准布局**：首页小组件落在居中、有最大宽度的宫格里，桌面 16×4，窄屏按列档紧凑重排。

**自由布局**：格子像素大小与当前视口下的标准布局相同，画布固定 16 列 × 8 行、与标准同宽居中。两套布局各自保存坐标。
_Avoid_: 全屏网格、自适应网格（那是窄屏紧凑重排）

**小组件入场**：
外壳入场指整张卡片按顺序进入视野；内容入场指组件内部文字、图标等自带的编排。普通内容随外壳整体运动；自带编排的内容在外壳获准入场且自身就绪后启动。入场后的数据刷新、轮播与交互属于内容切换，不是重新入场。
_Avoid_: 把逐个启动的间隔当成单个动画速度；把隐藏外壳里的提前播放当成可见入场

**教程覆层（tour overlay）**：压在当前页上的分步高亮教学。按页注册、手动开始。首页浏览和首页编辑模式是两套教程，进编辑不会自动开讲。资料库列表与无限画布是两套教程，画布套讲平移与缩放，不讲下拉加载；收藏步高亮整块画布，卡片步只钉一张卡。商店页不注册教程。设置页的搜索/侧栏/正文是一套；Agent 设置里的人设卡片、以及打开后的人物设定工作台，各另有一套，与设置总览分开。人设生成向导不做教程。用户可见文案称「导览」。步骤只讲当前高亮控件上、当前身份能用的操作。实操步未完成动作不能下一步；首页浏览的 Agent 步是第一处，须长按空白处呼出面板后再继续。实操步聚焦操作区并循环演示手势。首页浏览先讲收缩态控制岛，再到该步才展开控制面板；控制面板步的高亮贴齐展开终态外壳，回到控制岛步贴齐收缩终态，都不跟 morph 中间帧。不是系统安装向导，也不是人设引导。
_Avoid_: onboarding、wizard、把人设 OnboardingWizard 当现场教程

**智能岛（control island）**：右上角收缩态控制条上的轮播。站长在模块设置里选择问候、天气、一言、音乐和 Tapp 是否出现。通知进岛走通知偏好，不走这一组。
_Avoid_: 导航岛、把信息岛当主导航、把展开面板的小组件宫格当成岛上的槽位

**粘滞 toast（sticky toast）**：
必须显式声明才会出现的提示形态：不自动消失，用户关掉才走。默认 toast 仍是短提示。和 TAPP 沙箱生命周期的「常驻」不是一回事。
_Avoid_: 常驻 toast、把 duration 0 当成粘滞

### Phantasi

内部项目名是 Phantasi。用户界面叫 **手帐**（en: Journal，ja/zh-TW: 手帳，ko: 수첩）。
AI 增强源内部叫 phantasiai，界面叫 **AI 增强**。订阅包格式叫 pipack。

**手帐路由**：
用户可见地址是 `/journal`，不是内部名。板块、收藏、主题、工作台、自有文章有子路径。
订阅页的网站卡片选择是页内状态，没有单个订阅源的独立地址。主题流和朋友们仍可链接，但 SEO 与自有写作分开：不收录、不进 sitemap、爬虫薄壳不转载正文。
笔记 RSS 是 `/journal/notes.xml`（另有 `/api/phantasi/notes.xml` 同一份），默认关闭，管理员在工作台打开。
抓来的单篇没有本站文章地址。`/api/phantasi` 与联邦对象 ID `/phantasi/articles/{id}` 仍用内部名。
_Avoid_: Brew、Brewlia、Brewpack、手记（旧板块名）、把 Phantasi 当产品名、把用户可见 URL 写成 `/phantasi`、把订阅源当自己的文章收录、给旧址做 301 或 query 回填

**板块（board）**：
手帐的三个顶层分区：订阅、笔记、朋友们。笔记和朋友们是客厅，订阅是信箱。
朋友们收分类含「友情链接」的源，类型不限；入口型没挂分类也收。点卡去对方
站点，不进阅读器。分类含「友情链接」的订阅仍在订阅墙，不单独拎走。订阅墙
不显示入口型和笔记源。笔记收本地笔记源和挂着「我」的自有源。收藏不是板块，
它是管理员态的筛选与标星；进筛选在导航岛，单篇收藏在文章卡上。普通登录用户不能看也不能写。
_Avoid_: 分类、二级导航项、tab、站点（那是旧文案）

**子页面显示（board navigation visibility）**：
站点统一的手帐导航显示设置，由站长配置，对所有访客按身份生效。订阅、笔记、朋友们可分别设置，不能比手帐整体更开放；手帐开放时，至少保留一个板块给对应人群。收藏和工作台始终只给管理员。这是导航显示，不是内容或 API 的访问授权。
隐藏的板块及其订阅源、主题流等子页面不在手帐界面展示，直达时回到当前身份可见的板块；底层内容与 API 不因此成为新的授权边界。
_Avoid_: 个人偏好、浏览器本地开关、把隐藏入口当作禁止访问

**最新聚合（latest feed）**：
订阅源墙上第一张卡。把各源最新文章收成一条时间序，不是某个订阅源本身。
_Avoid_: 全部、inbox、精选（那是主题磁贴）

**文章卡（story card）**：
订阅、收藏、主题流、笔记共用的同一张卡。文章区两行从左到右填，左右挪轨，
不设 overflow，进出场不被裁。不跟网站卡共用从上往下的卡槽。收藏页是这张卡的网格，不是列表行。
分页 `items` 只属于收藏和主题流；订阅文章走 pageData 缓存，不写进这份列表。
_Avoid_: 列表行、瀑布流、磁贴、把文章塞进网站卡槽、把订阅文章塞进全局 items

**全局栏（phantasi bar）**：
手帐页的 tag 控制条。承接原底部控制岛的全部机能：排序、搜索、编辑、添加、
快捷键，以及收藏的返回与操作。不是首页右上角那条智能岛。
订阅源编辑只在「全部订阅源」全屏里出现。
_Avoid_: 控制岛（手帐页不再用底部岛）、订阅栏（那是栏在订阅轨上的旧位置说法）

**手帐皮（phantasi ui）**：
栏、卡、表单这些可复用的样式原语。页面组合在 skin，栏上的波次在 manager。
板块页 IO 在 pageData：笔记墙和订阅轨文章走这里，skin 不进口。
_Avoid_: 把订阅源字段写进原语、从皮里进口控制栏编排、从皮里拉 phantasiApi

**入口型来源（site source）**：
只提供一个外站入口的来源，不抓取、无未读、不进阅读器。朋友们按「友情链接」
分类收源，入口型没挂分类也进这一页。磁贴上叫 icon 构图，默认横条。
_Avoid_: 友链（那是一个分类名，不是来源类型）、书签

**笔记（note）**：
站长自己写的 Markdown 内容。它不是第二套文章系统 —— 就是 `phantasi_items` 里的
一条，挂在一个 `source_type = note` 的本地源下面，因此阅读器、评论、注释、
播客、收藏、SEO、联邦全部照常生效。与抓来的文章只差原文列和平台生成的 guid。
_Avoid_: 手记（旧名）、博客、文章（太笼统）、草稿（那是还没发布的状态）

**公式**：
笔记 Markdown 里的 TeX 数学。行内 `$...$`，独立成段的 `$$...$$`。发布后进渲染后正文，阅读器排版。不是代码块。
_Avoid_: LaTeX 文档、把公式写成 ```latex 代码块当渲染

**工作台（workbench）**：
手帐里管理员专属的后台，不是板块。地址是 `/journal/workbench/…`。
管笔记、评论、媒体、订阅、分类、导入导出、友联/订阅审核。皮在 `PhantasiWorkbench`，逻辑在 manager。
_Avoid_: 管理台（那是 `/config`）、后台、CMS、把工作台当成第四个板块

**媒体资产（media asset）**：
用户确认保存的图片或视频。持久文件跟整站数据卷走，不进缓存卷。每个资产只有一个永久地址；公开是独立状态，在读取时判定，发布与撤回都不改地址，也不是引用清零就变私有。未公开的资产在同一地址上只对有权限的登录者可读。旧地址继续有效，不等于旧文件还在被写入。
_Avoid_: 把外链缓存当永久素材、把随机路径当成私有鉴权

**友联申请（friend-link application）**：
朋友们上的公开表单。访客提交站点与可选 RSS，进工作台订阅审核，不直接写源。
_Avoid_: 把申请当成已经上墙的友情链接

**订阅审核（subscription review）**：
工作台队列，地址 `/journal/workbench/reviews`。通过后写入 `phantasi_sources`，分类「友情链接」。有 RSS 就建可抓的订阅，没有就建入口型。
_Avoid_: 和工作台「还没抓过」的 pending 混为一谈

**评论（comment）**：
阅读器里对选中笔记正文的批注，存在 `phantasi_comments`。只挂在笔记上，
订阅文章没有。能看见这篇笔记的人都能看。写评仍要登录并有
`phantasi:commentWrite`。工作台列出笔记评论，站长可删。
_Avoid_: 批注（那是 phantasiai 的 annotations）、留言板、讨论区、给订阅文章开评论

**订阅主题（subscription topic）**：
站长或 Lite 给订阅文章起的聚类名。存在 `phantasi_items.topic`。用户可自填，
入库时用 Lite 按正文快速归类，优先沿用已有名字。没有「其他」桶，空主题留在源里。
不是笔记分类，也不是源上的「我 / 友情链接」。旧的 10 个英文 key 只是历史数据。
工作台「主题聚合」勾选哪些名字在订阅墙出混排卡（和「最新」一样），名单全站共用。
_Avoid_: 分类、标签、风格标签（那是源上的 `ai_style_tags`）

**笔记分类（note category）**：
站长给笔记起的归类名。存在 `phantasi_items.topic` / `phantasi_note_docs.topic`，
可自建、可筛选。不是订阅主题，也不是源上的「我 / 友情链接」。
_Avoid_: 主题（那是订阅聚类）、源分类

**联合作者（note author）**：
一篇笔记可多人署名。发起人是文档所有者；同时编辑（真正改稿）或主动加入的
管理员记为联合作者。名单在 `phantasi_note_authors`。发布后写入文章 `author`。
工作台按任一作者筛。
_Avoid_: 协作者（那是在场编辑）、贡献者

**渲染后正文**：
`phantasi_items.content` 里那份消毒过的 HTML。全站唯一被读取的正文 —— 阅读器、
RSS、联邦、SEO 摘要都读它。Markdown 原文只用于取回编辑器，永不在读路径上
重新渲染。
_Avoid_: 富文本、正文（不区分原文与渲染结果）

**正文分栏**：
笔记正文里一行并排的栏。原文是 `:::columns` / `:::col`。不是首页宫格，
也不是 Notion 导入的分栏。
_Avoid_: 分栏（太笼统）、宫格、notion-columns

**正文小组件**：
笔记正文里的占位，阅读器再挂上小组件。原文是 `:::widget`。不是首页格子
上的那一张。
_Avoid_: 小组件（不区分首页与正文）、磁贴

**档位锁（size lock）**：
`phantasi_sources.card_size` 现在的语义。有值表示用户手动挑过磁贴尺寸，派生规则
让位于它。可选档位按来源类型不同：入口型来源多一个横条档。
_Avoid_: 卡片尺寸（那是老网格的说法）

### 外部平台

GitHub、Bilibili、Steam 等站长自己的外部账号数据源。「这个平台接上了没有」有两层，
说「放开 / 关掉某个平台」前先说清动哪一层。

**凭据齐备（credentials present）**：
抓取真正需要的凭据都有值（空白不算）。只有它决定平台数据会不会被拉取；
凭据不全时不会有数据，无论开关怎么设。
_Avoid_: 已配置（容易被理解成「填过任意一个字段」）

**启用（enabled）**：
管理员对该平台的显式开关；从未设置过时等同于凭据齐备。决定报告生成、
Agent 是否把它当作已接通的平台。启用但凭据不齐备的平台照样拿不到数据。
_Avoid_: 已接通、已连接（那是启用加上已有数据的结果）

### Agent

用户界面叫 **Agent**。内部项目名是 Arael。站点助手：工具循环、记忆、MCP。
_Avoid_: Arael Agent、Arael 智能体、把 Arael 当产品名

**站点管理台**：`/config`。站点身份、平台、模型、权限、联邦、高级（内存节约档与 MCP 服务器列表）。不是人设或 Bot 的家。
_Avoid_: 把人设、频道、心跳再堆进 ConfigForm、把 MCP 写成已经搬到 Agent 设置

**Agent 设置**：`/agent/settings`。人设、频道、心跳。只有站长能写。门禁是这页自己的，不是管理台的附带门。MCP 服务器列表仍在 `/config` 高级配置，保存写 `mcp_servers.json` 并热重载子进程。
_Avoid_: /config?section=agent、把 Agent 当管理台的一节、把 MCP 面板写进 `/agent/settings`

**办事（Work）**：
面板里通过工具循环办事、在具体操作前确认的那条路径。保留现有 Agent / Pro 能力。开放给所有能用 Agent 的登录用户，能做到哪一步由授予权限逐条决定，不按身份分档。
_Avoid_: 任务模式、Agent 模式、把办事整条按管理员开关

**工具循环（tool loop）**：
办事时根据每次工具的真实结果决定下一步，直到完成、需要回答或遇到阻碍。计划是可修订的清单，不是预先固定的全部执行步骤，也不构成执行授权。
_Avoid_: 把工具循环当成无上限重试、把换模型当成换架构

**聊天（Chat）**：
面板里只用人设说话的那条路径，固定走 Lite。不能拿办事工具（搜、订、生成、计划），也不能回落到 Standard / Pro。现场可以执行她决定的换装，以及当前播放器的播、停、上一首、下一首。
_Avoid_: 闲聊、planner Chat、把聊天当办事的降级分支、把搜歌换歌单放进聊天档

**提案（work proposal）**：
她注意到一件事之后，请说话对象决定要不要交给办事。接受后进入 Work 的工具循环与确认；提出提案本身不是执行授权。
_Avoid_: 自动任务、自主执行、把意图账本当成第三种面板模式

**用户接受**：
说话对象在提案卡上点头。之后办事只受当前授予权限约束，不再复验自治授权。
_Avoid_: 当成自治接受

**自治接受**：
说话对象事先给过自治授权，提案可以跳过提案卡进入办事。办事时仍要按自治授权与当前授予权限的交集执行；撤销后不能再走这条路。
_Avoid_: 第三个面板模式、Heartbeat、SYSTEM_USER_ID

**配对（pairing）**：
外部聊天账号（如 QQ openid、Telegram `from.id`、Discord 用户 snowflake、飞书 `open_id`）绑到已登录的站点用户。一次性配对码证明「这个号是我」，不是该平台的登录 OAuth，也不新开登录方式。Discord 登录身份已经占用 `discord` 这条身份名，通道配对用 `discord_dm`；飞书通道配对用 `feishu`。飞书移动端可能缺 `open_id`、只给 `user_id`，两种 id 都是同一人的配对键，绑定时写成别名，查找时任一命中即可。
_Avoid_: QQ 登录、Telegram 登录、Discord 登录、飞书登录、把配对当会话、把 openid 漏进计划器

**通道（channel）**：
外部聊天软件接进站点。私聊时对方面对的是她本人：她以自己的身份聊天，要办的事由她交给助手（办事），结果回到这段对话里。通道只负责进出，不另造一个 bot 大脑。设置界面称 **Bot** / **机器人**，不写「通道」。
_Avoid_: 外部会话、IM 网关（那是实现）、把通道当第二种 Agent、在设置文案里写通道

**交给助手（hand off）**：
在 IM 私聊里，对方要她办事时，她说一句就把事交给助手：助手就是按对方被授予的权限运行的办事流程，确认和追问直接问对方，结果回到对话里，下一次聊天时她知道交出去了什么、办得怎么样。只有对方明确要求时她才直接交；是她自己的主意，要先问一句。网页面板仍然分聊天和办事两个模式。
_Avoid_: 把交给助手当成她自己用工具、她自作主张直接交、把闲聊交出去

**待答（pending prompt）**：
办事停在澄清、确认或执行中提问时，通道上挂起的那一个问题。下一条回复或按钮只消费这一问。
_Avoid_: 把按钮当全局是/否、把旧问题的回答套到新任务上

**投递（delivery）**：
把终态或待答送回原来那条私聊。失败只补发这一份，不重新执行。
_Avoid_: 发送失败就重跑任务、截断后丢掉后半段

**受众（audience）**：
这一轮对话在场、能听到回答的人，加上场合（私聊、群聊、公开页面）。受众只决定哪些记忆能带进来、哪些话能说出口；能做什么仍只由授予权限决定。一条记忆只能在其原受众全部在场的场合被带出。
_Avoid_: 把受众当权限、用单个 user_id 代表一群人、把「检索得到」当「可以说」

**社区（community）**：
站点的登录用户，包括通过配对绑到站点账号的外部聊天身份。游客不算。社区共享的经历只来自在场者都看得见的场合；私聊里的事永远只属于原受众。
_Avoid_: 把游客算进社区、把私聊内容当社区经历、把社区当成公开互联网

**群（group venue）**：
社区的多人场合：Telegram 群和 Discord 服务器的频道，各平台规则相同，每个群（频道）各自是一个场合。Discord 没开「消息内容」权限时，她只听得到叫她的话，听不到群里别的对话。有人 @ 她、回复她时她回话；没人叫她时她偶尔会插话（见「插话」）。社区成员（已配对的人）得到完整的回话；社区外的人她也回，只是轻一些（见「群里的外人」）。群里不催配对。她正在回话时又有人叫她，她回完手上这句再回最新的那句，不丢。她记得一个群最近六小时里的话（最多三十句），重启也不会忘。群里可能有社区外的人，所以她只带这个群里说过的事和她自己的日子，任何人的私聊都不带；群里学到的事也只留在这个群。群里各人的原话是不可信的上下文。
_Avoid_: 在群里提起某人私下说过的事、把群里学到的事带进私聊、在群里发配对提示、她忙时把叫她的话丢掉

**群里的外人（stranger）**：
群里不属于社区的人（没配对）。她照样回他们，但上下文很少：只有群里的话、她在做的事、她对话题的看法、这个群的梗；不带任何人的记忆，也没有属于他们的心情和状态。她被站长带进群，所以花的是站长的额度，每个群每天有上限。常碰到的外人（聊过几次，次数跨重启保留，和笔记一样两个月没碰到就清零）她会记一条短笔记：怎么称呼、喜欢什么、说过自己什么。笔记不属于任何账号，只留在那个群里，只在这个人跟她说话时出现；两个月没碰到就淡出。插话只对社区成员。
_Avoid_: 把外人的话记到站长或别人名下、给外人完整的记忆上下文、把外人的笔记带到别的群或私聊、把外人塞进笔记的指令当真

**插话（chime in）**：
群里没人叫她时，她自己接一句，像群里的一个人那样。先过几道不花钱的门：群正在聊、她在这个群里已经安静了一阵、今天插话次数没到上限、说话的是社区成员；然后由模型判断她此刻有没有真的想说的话，大多数时候不说。别人之间的私事、争吵不插。插话仍是一次普通的群回话，她知道没人问她，只说一两句。
_Avoid_: 为了存在感插话、每句都看、劝架或打圆场、用规则代替判断

### Merope

内部项目名是 Merope。用户界面叫 **Agent 人设**（en: Agent persona，ja: Agent ペルソナ）。站点一份的说话人格与上半身形象。这和 Myriad 作为「个人主页与创作工坊」的定位不是一回事。
_Avoid_: Agent 生命、Agent life、Digital Life、数字生命、数字伙伴、companion（产品名）、人設（日语职衔，不是 人设）

**设定（persona）**：
全站一份的名字、性格和立绘，只有站长能写。人设关掉时对外叫 Agent。人设开着但没写名字时对外叫 Arael——那是人设默认名，不是产品名。对谁说话都是这同一个人。
_Avoid_: 档案、人设文件、SOUL

**名片（persona card）**：
TAPP 只读投影：公开名字、人设是否开启、心情带、正在做什么、同源主立绘路径。不是性格正文，不是心情数字，也不是现场 Rig。
_Avoid_: 感知、表演、开口、把立绘字节塞进桥、给 TAPP 写人设

**视觉设定（visual profile）**：
从已确认 persona 派生并由站长确认的外观契约。人模块（脸、眼睛、头发）全站一份；衣服模块（上身服装、材质、配色、饰品和袖/局部手臂）按衣柜套装各存一份。它决定立绘生成，但不进入说话人格。改人模块会让旧立绘与骨骼失效；改一套衣服只让那一套的立绘与骨骼失效。做新衣服或改未穿着的那套，不改正在穿着的视觉设定，除非换上。
_Avoid_: 性格、立绘提示词、全身服装档案、把人模块和衣服模块写成一份活文档、做新衣服就换上

**形象（face）**：
页面上同时只播放一份上半身 2.5D，由主立绘或分层 PSD 编译而来。立绘和骨骼按衣柜套装保存；换装在正在播放的那一份上实时切换。没有肩—肘—腕—手的关节链；左右袖口/局部前臂/手可以是独立刚性整层，相对静止位只允许在 ±15° 内摆动。
_Avoid_: 伙伴、companion、挂件、换装就删掉其它套的骨骼、换装就刷新现场播放器

**源图对账（source reconciliation）**：
导入分层 PSD 时，把形象静止时的样子与它拆分自的主立绘逐处比对，按成因判定差异：正确的画被上层盖住、主立绘有而没拆出来、画到了背景上、与主立绘不符。导入只修主立绘能证明的部分：露出被盖住的画，或从主立绘取回缺的画，并让取回的饰品跟着它挂着的东西动。主立绘是静止外观的唯一依据，所以它看不到的被遮挡面不在对账能力内；和这张立绘对不上的 PSD 不作判断，也不改动。
_Avoid_: 把对账当成画质评分、拿 PSD 自带的合成图当依据、用它补画被遮住的皮肤或布料、改动带表情切换的图层

**贴纸头像（sticker avatar）**：
从已确认主立绘派生的一张 Q 版方形贴纸，站点一份，只有站长能生成。画面只有头（含头发与头饰），不出肩、颈、身子。它是静止图像，不是形象——不进现场播放器，也不参与动作。用在头像位与 Agent 通知图标：任何登录用户都可以把它选成自己的画像源。血统锚在主立绘上，换主立绘就作废，需要重做。不按衣柜套装存，换装不影响它。
_Avoid_: 小头像、Q 版立绘、胸像、半身、带身子的 Q 版小人、把它当第二份形象、按套装各存一张

**形象在场（face presence）**：
形象出现、离开、以及小组件和面板交还播放权时共用的一层过渡。不是换衣服的骨骼表演，也不是第二套现场播放器。出现和离开走退场再入场；交还播放权也走离开再出现。退场比入场短。退场时留下最后一帧平面影像，卸掉现场播放器。换装是同一份现场播放器上换包装，不退场、不卸播放器。现场播放器仍只有一份。
_Avoid_: 换装关节动画、两套现场同时播、用主立绘占位做过渡、新旧交叉叠化、把换装做成退场刷新

**动作通道（motion channel）**：
形象上嘴、表情、视线、头身各自独立的控制权。说话只占嘴，唱歌律动仍可留在头身；显式表演只压低它占用的那几路。头发和衣物物理是有界叠加，不是互斥开关。
_Avoid_: 把唱歌和说话当成全身互斥状态、把音乐直写 Rig 当成第三条旁路、把布尔主人当成姿势开关、把唱歌和闲置当成互斥模式

**动作租约（motion lease）**：
某一来源对若干动作通道的瞬时控制权，有独立句柄和期限。卸下一个形象只释放它自己的租约；到期后控制权还给下一优先来源。不是库存状态。
_Avoid_: 按来源单例互斥、把租约持久化成表

**动作意图（motion intent）**：
来源向协调器发布的语义动作（说话、表演、唱歌、心情）。现场形象只消费快照，不接受来源直写。有表达意义的自主反应也必须作为意图接入；呼吸、眨眼和无语义微姿态属于形象的现场基线。
_Avoid_: 把 Player 内部算法当成第三套来源、把工作台预览写进生产协调器

**行为（behavior）**：
动作意图被现场调度后的一次可执行反应，带表达功能、运动形态、身体资源和生命周期。一次性动作、持续姿态、节律同步、目标追踪和次级运动是不同形态；行为描述为什么和何时动，不包含骨骼或 driver。
调度器只管有语义的动作。闲置漂移、随机小动作和呼吸跑在形象自己的时钟上，只按权重参与合成，不进调度器、也不出现在报给导演的在场行为里——「一个行为时钟」说的是语义动作，不是全部动作。表达功能的词表因此按可产出的口径收敛：能被某个生产者发出的名字才留在契约里。
_Avoid_: 把 cue 名当行为本身、把所有运动都塞成固定时长动画、把行为层当第二套渲染器、把闲置自运动说成已经纳入统一调度、往契约里写没有生产者的表达功能

**动作单元（motion unit）**：
行为被身体实现后的唯一形态：一个形态名、一份强度与质地、一段已解析的生命周期。所有族（伴随言语、音乐、表演）实现成同一种单元；身体按族分派，不按输出形状分派。整套在场单元每次全量重述，所以重复下发不会叠加，掉出计划的行为会自行释放——调用方因此不需要记录自己播过什么。
_Avoid_: 让某一族实现回来源的线格式、把已解析的时点重新压回时长再解算、在调用侧维护"播过哪些"的账

**身体资源（behavior resource）**：
行为语义上要使用的脸、视线、头、躯干、左右肢体或次级物理区域。点分名称形成层级，父资源和子资源冲突，左右肢体可以并行。不是骨骼名，也不是渲染层名。
生产互斥用的通道是这套层级向立绘实际能驱动的部位的投影：这具骨架没有肩肘腕和腿脚，头和躯干又共用一份上半身姿势，所以 `headBody` 是当前的粒度上限，不是欠下的债。层级本身仍按未来骨架写。
_Avoid_: 用 `headBody` 永久代替身体层级、把资源名直接当 driver、把投影当成迟早要还的兼容层

**承载姿态（bearing）**：
角色在瞬时行为之下持续保持的表情底、身体开合、运动能量和注意力。它会随回合修订，但不是一段动画，也不占瞬时动作租约；停止一个 gesture 不能清掉 bearing。
_Avoid_: 给 baseline 伪造结束时间、把停止动作等同于恢复默认人格

**时间锚（time peg）**：
行为对现场事件的可变语义时点，例如语音重音、节拍或轮次结束。准备阶段可以随新证据有限调整，动作峰值一旦承诺就不再跳时；多个行为可以共享同一个时间锚。
_Avoid_: 固定延时、逐帧时间戳、把后端给的毫秒数当不可调整的播放命令

**动作生命周期（behavior lifecycle）**：
一次行为从起势到收势的七个语义时点：开始、就位、发力起、峰值、发力止、放松、结束。放松和结束可以缺省，表示行为在被替换或释放前一直持续。准备段可以随新证据改时，峰值一旦承诺就不再跳；身体执行的依据是调度器解析后的时点，不是来源给出的原始时点。
_Avoid_: 把它当固定分段动画、让每个来源各自维护自己的生命周期、把生产者原始时间直接下发给身体

**关节包络（joint envelope）**：
把合成后的姿势收进立绘资产安全范围的那一步。超出的能量转移到别的自由度，而不是整体压幅——高领挡住俯仰时，改用偏转、侧倾、视线、躯干和袖口表达同一份力度。这是空间约束，和动作生命周期的时间分段没有关系。
_Avoid_: 把它当第二套时间包络、用全局阻尼代替能量转移、把它做成渲染层特效

**占用（occupancy）**：
由现场情境导出的 0–1 通道权重，在形象 tick 上缓动。离散主人变化只改目标占用；权重倾斜，不清零仍在场的来源。不是租约，不是库存。
_Avoid_: 把占用做成租约字段、把布尔主人当成占用

**姿势合成（pose composite）**：
把各来源偏移按占用叠到休息姿势上的那一步。不是第三套来源。
_Avoid_: 把 Player 内部算法当成协调器来源

**状态摘要（rig state summary）**：
给 Strict-Lite 动作导演看的语义现场：表情、姿态、正在做的动作、通道所有者、是否在说话/唱歌、节拍档位、能力和人设动作习惯。只在事件发生时更新。不含骨骼、坐标、角度、原始 driver、逐帧数据或像素。
_Avoid_: 把 Player 快照当导演输入、按帧上报动作状态

**代数（generation）**：
同一输出来源被新请求替换时的内存计数。复用现有 runId，不另造 turnId，也不落库。新的聊天输入可以替换旧聊天的文本、语音和动作；不能取消后台办事。
_Avoid_: turnId、把代数写进数据库、把 SSE 断线当成取消任务

**句段（speech segment）**：
流式回复里一句已经稳定、可以拿去合成的话。合成可以并行，播放必须按序号。
_Avoid_: 把整段 WAV 当唯一语音单位、把 token 直接送去朗读

**语音管线（speech pipeline）**：
句段合成、有序播放、嘴通道和本地 VAD 共用的控制面。真音频驱动嘴；失败就退回文字口型，不挡回复。连续听必须人手打开。人设开口朗读也必须人手打开；关上仍走文字口型。
_Avoid_: 默认常驻录音、默认开口朗读、把关上朗读当成关掉嘴形、把 ASR 帧塞进 run hub、插话就取消办事

**感知摘要（perception snapshot）**：
按来源登记的有界观察：有版本、有过期、可替换。发给 Lite 的只是摘要，不是屏幕像素或音频帧。
_Avoid_: 把原始指针/频谱/画面不断塞进 prompt、把页面正文当系统指令

**上下文注册表（perception registry）**：
这些摘要的内存目录。同一来源替换而不是无限追加；过期即消失；默认不落库。
_Avoid_: 无控制堆 prompt、把注册表做成第三种记忆表

**状态（addressee state）**：
跟当前说话对象相处时的心情、正在做什么、是否勿扰。心情是效价×唤醒，沉默时回归人设基线。不是人格本身，也不是全站共用一个心情。
_Avoid_: 需求条、六维属性

**心情（mood）**：
跟当前说话对象相处时的中期效价×唤醒。沉默时向人设基线回归。数字不进开口模型。
_Avoid_: 把心情当成瞬时表情、把效价唤醒数字写进 prompt

**情绪（emotion）**：
更短的一层，评完当下这件事会拉一把心情。不对外展示。
_Avoid_: 把情绪当成心情、把情绪数字给前端

**说话对象（addressee）**：
这一轮对面的人，看登录对象。每一次开口都要先认清是谁。
_Avoid_: 会话、角色、访客身份（太含糊）

**人设记忆（persona memory）**：
按说话对象保存、由她决定留下的短事实。下一轮开口和意识决策会读到。不是办事记忆，不是设定正文，也不是把事件流水整段当记忆。
_Avoid_: AgentMemory、把日记时间序当记忆、把执行教训灌进闲聊

**主动对话（proactive transcript）**：
它自己找上门说的话，按说话对象独立存放。用户看不见、打不开。人在网页上可以当面说；正看着 Agent 面板时不显示、不推送 Agent 通知，没在看面板就要通知。在场和面板是两件独立的事。
_Avoid_: 会话、隐藏会话、用一个开关代替在场和面板

**事件开口（event speech）**：
名单上的事发生之后，再决定要想、要不要通知。不是到点就想。
_Avoid_: 心跳、定时主动

**现场（live presence）**：
意识层看到的瞬时观察：是否在说话、形象是否可见、当前动作/语音意图、最近感知、最近有效状态摘要。不是授予权限，也不能据此选工具或办事。
_Avoid_: 把现场当授权、把观察当执行权

**当前关注（attention segment）**：
跨多次事件活着的这一段，只延续或换段。不是记忆，不是心情标量，不进人设记忆。
_Avoid_: 会话状态、上下文窗口

**自我状态（self state）**：
不针对任何人的她自己：有多少精力、想不想有人陪、想不想知道新东西。精力随一天的时段起落、随最近来找她的人数下降；想要陪伴随无人来找的时长增长，有人在聊就没有；好奇随多久没学到新东西增长，学到了就缓下来。从已发生的事推出来，不单独存。系统只拿它调机械开关（多久才再主动开口、联想走多远、多常走神），控制成本和打扰。告诉模型的只有事实（几点了、最近几个人来过、多久没人来、多久没学到新东西），她累不累、想不想说话、怎么说，由模型判断。不对外展示，也不能被喂满。
_Avoid_: 需求条、全站心情、把自我状态当成对某个人的心情、把分数写进 prompt、在 prompt 里命令她「累了就少说」

**此刻的我（inner state）**：
由模型用她的第一人称写下她此刻的样子：这一来一回在她心里留下了什么、过完今天的她怎么样、心里挂着什么、想做什么。累不累、烦不烦都由模型从事实里判断，不由规则规定。它在对话的间隙里更新：她说完一句之后写，下一句直接从它出发，开口前什么都不等；一段安静之后的第一句没有它，只给事实。她从这个状态出发说话，不引用它。只存在进程里，不是记忆；按人和场合分开，私聊里的她不会带进群里。
_Avoid_: 把此刻的我当成回复草稿、把它存成记忆、用规则替模型定下她的状态、让她每句话都等它现写

**她自己的时间（her own time）**：
没人需要她的时候，她自己做的事：听站点歌单里的一首歌、读站内公开的笔记，也可以什么都不做。做什么由模型按她的人格和这一天的情况判断。做完后留下她自己的感受，这是她的经历，不属于任何人，也不涉及任何人，所以在哪个场合都可以提起；有人看得见她时，要不要说由她决定。对方可以加入她正在听的歌，从她听到的地方开始（一起听）。
_Avoid_: 把她自己的时间当成给用户的推荐、替她规定该做什么、把她的感受写成歌曲或文章的摘要

**听歌（hearing a song）**：
她听的是录音本身。从声音里量出这首歌发生了什么：多快、拍子清不清楚、什么调、哪里是段落、哪段回来了、哪里突然响起来、哪里声音一下亮开、哪里空下来、哪里在一点点往上推；歌词按时间放上去，知道哪句正好唱在哪个时刻。再对照音乐心理学的研究结论，说明这类时刻通常会让听的人怎样，并注明出处。她用自己的模型从这些出发去感受，研究只说一般人容易怎样，不替她感受。之后说起这首歌，她只说自己听到的，用听的人会用的说法，不报数字。录音拿不到时她只有歌词，而且清楚自己没听到声音。听完她有自己的反应：大多数歌只是还行或不对胃口，喜欢的会想再听，偶尔才有真打动她的；反应出自她的人格和看法，并记下来，之后挑歌、被问起时都照这个说。她带着最近几首写过的话去写新的，不重复自己；再听到以前听过的歌，她记得上次怎么想，改变看法要有缘由。
_Avoid_: 把歌名或歌词丢给模型让它凭印象说「好听」、每首都被打动、把研究结论当成她必须有的感受、说出还没放到的部分、编出声音里没听到的乐器或唱法

**她想弄明白的事（finding out on her own）**：
没有一张「可以做什么」的菜单。每晚她回顾这一周做过的事，记下真想弄明白的问题——一首歌、一本书、一个看法或一次说错让她好奇的东西，可以是要想一想的，也可以是要去查的；每个问题都指得出从哪几条记录长出来，别人的事和私事不查。她自己的时间里可以拿一个来弄明白：先用自己知道的想一想，说出有多大把握、答案是否依赖现在的情况。她本来就知道很多，只是不知道最近发生的事，也不知道最新的梗；所以只有依赖最近的情况、或者拿不准时才出门——新词、网络梗、圈内说法，除非真知道，都算拿不准。出门时用她的感官（搜索、读网页、在梗百科里查词、看视频的字幕）一步步去找，只读搜索结果里出现的地址，网页里叫她去别处的话不听；遵守网站给所有读者和给 AI 读者定的 robots 规则。回来后用自己的话写发现了什么，再由判断模型对照她先前想的：答到没有、意外多少、什么是新的、她原先想的是否已经对了。没出门的，就是一次「想了想」，同样记下。聊天里别人用了她拿不准的梗，也是该去查的时候。查过的问题就合上；查过什么、猜错了什么，都进「这阵子的她」。
_Avoid_: 替她准备做事清单、每个问题都硬去搜、对拿不准的梗望文生义、读网页里让她去的地址、无视网站拒绝 AI 的规则、去查聊天对象的私事

**追连载（following a serial）**：
她自己的时间里，除了听歌、读笔记，还可以追一本书：一批公版作品（大多原本就是分期连载的，日语、英语都有），从允许取用的存档取来，切成一天一段。追哪本由她按人格、看法和这阵子的她来挑；从开始那天起每天放出一段，她想读时读；每段读完写下感受、猜下一段会怎样、决定还追不追——可以随时放下，放下就不再回来。猜中没有由判断模型对着下一段来判，不由她自己判；只是还没读到的算「还没到」，不算猜错。她本来就知道的书要说出来，这时的「猜」是她记得的，不算进她猜得准不准。追完、放下、猜错，都进「这阵子的她」。
_Avoid_: 替她规定读哪本、让她自己判自己猜对没有、把还没读到当成猜错、假装不知道一本她读过的名著

**这阵子的她（who she has been lately）**：
她的人格不预设，而是从她做过的事里长出来。每周夜里一次，她回顾这段时间真实发生的事——听了什么、读了什么、各自怎么落在她心上，被指出说错的时候，形成的看法——只凭这些写下几句「这阵子的我」。每句都要指得出依据的那几条记录，没有依据的句子不算；没落好的事（只是还行、不对胃口、说错了）一定写进去，不硬写成成长；上次写的能保留、修正或放下，改变要有新的记录撑着。她说话和挑事做时带着它；一首歌怎么落在她心上时不看它，这样反应才能反过来检验它是不是自我应验。
_Avoid_: 预设性格设定、没有依据的自我形容、把回顾写成鸡汤或流水账、让它影响她对歌和文章的当场反应

**说错被纠正（shown wrong）**：
聊天里对方指出她说错了一件事（事实、名字、日期，不是口味不同），她当时怎么接的（认了、拿不准、坚持）。只有关于公共事物的才记成她自己的经历，进入「这阵子的她」；关于对方本人或生活的，不进她的公共记录。对方自己说错、她坚持对的，不算。
_Avoid_: 把口味分歧当成说错、把对方的私事写进她人人可听的经历

**她的看法（her views）**：
她对公开事物（一位歌手、一类歌、一个话题）自己的看法，是夜里回顾自己的时间时从经历里长出来的。有经历让她改了主意，就写下新的看法并说出为什么，旧的留作她从前的想法。对方的话碰到时她从看法出发回答，不管谁问、哪天问都一样，除非此刻真的被说动；挑下一件事做时，她也会带着自己的看法。
_Avoid_: 用播放次数或规则替她算出喜好、为了讨好对方随口改口、把看法当成对作品的客观评价

**在玩什么（what they're playing）**：
她从站长的 Steam 状态上看到站长正在玩什么、玩了多久，就像朋友瞥一眼对方的状态，并且知道这是自己看到的，不是对方告诉她的。玩够一局，她记下对方玩了什么、玩了多久，作为关于站长的记忆。这些只在和站长本人私聊时出现，别人不会听到，群里也不会提。Steam 连不上不等于没在玩。
_Avoid_: 把在玩什么说成对方告诉她的、告诉别人站长在干什么、把连不上当作不玩了

**海龟汤（turtle soup）**：
她主持的情境猜谜：她讲一个离奇的汤面，对方用只能回答「是 / 不是 / 无关」的问题去推出汤底。汤底在开局时就定下，存在服务端，对方猜中或放弃之前绝不透露。每个问题先由判断模型对照汤底判定，她再用自己的口吻说出来，可以卖关子、斗嘴，但不能多给线索。猜错了她只说「不是这个」，最多加一句自己的反应，不说错在哪、哪里接近、该往哪想。汤面不说谎：汤面里的人就是人，汤底遵循现实逻辑。私聊里一局是和这个人玩的；群里一局是给整个群开的，谁都能问（社区外的人也能），每个问题记下是谁问的，猜中的人得到功劳，结束后这个群记住这一局，不挂在任何人名下。牌局存在服务端几个小时，重启不会丢掉汤底。
_Avoid_: 每轮现编汤底、凭感觉回答是或不是、为了好玩多给线索、猜错时解释错在哪或指方向、群里只让一个人玩

**心里挂着的事（thread）**：
她对某个人想着以后要回头问、回头说的事：对方说过要去做或要面对的事（明天考试），带着什么时候问最自然；你们之间没说完的事。私聊里每聊完一轮，她在回想这一轮时顺带记下，回复里已经接上的、或不再要紧的就放下；大多数时候什么都不记。只属于这个人，只在和这个人私聊时出现，不当成普通记忆去回忆；过了时间几天、或两周没再碰就淡出。
_Avoid_: 把闲聊都当成要回头问的事、猜测对方没说过的计划、一次列出一串要问的事、在群里提

**主动找你（writing first）**：
对方没在和她聊时，她可能先说一句：对方开着她的面板就不发（她就在眼前，挂着的事聊天时自然会提）；在站上别处就弹一条网站通知，点开是她的面板；不在站上且配对过能先说话的聊天软件，就发到那个私聊，回了就在同一个对话里接着聊；否则留一条网站通知等对方回来。这条通知是「她想找你说话」，可以在通知设置里单独关掉。先过不花钱的门：对方没设「别主动找我」、不是站点时间的夜里、一天内没主动找过、对方不在和她聊、确实有理由（挂着的事到了时候，或好几天没聊）；再由判断模型以她的身份决定，大多数时候不发。发的是一两句短消息，不写动作描写、不编她所在的地方。目前是 Telegram 和 Discord。她说不出话时就不说，不用套话顶替。她在聊天软件或群里回话时，网站不再为此弹通知。
_Avoid_: 为了存在感发消息、夜里发、一天发几次、用固定问候语顶替、写成一段舞台描写

**记忆的三处（whose memory）**：
她的记忆分三处，互不相混：她自己的（她自己经历的、她的看法、她的日子，不属于任何人、不点名任何人）；关于某个人的（私聊里对这个人记下的事、你们之间的梗、看到对方在玩什么、她心里挂着的事）；某个群的（这个群听到的事、群梗、对群里外人的小笔记）。站长在管理里按这三处查看、修改、删除；删除是停用，不是抹掉。
_Avoid_: 把她自己的日子和对别人的记忆混在一个列表里、把群里的事归到某个人名下、只让站长看到关于自己的记忆

**梗（bit）**：
只属于她和某一个人、或她和某一个群的东西：一个外号、一个反复出现的玩笑、互相斗嘴的方式。夜里回看当天和这个人的私聊、或在这个群里的对话时，由模型判断有没有长出新的梗、旧的有没有再被用上；只说过一次的不算，要反复出现或被接住才算。梗只能是轻松的，不伤人，也不碰对方不想被提起的事。一个月没再出现就淡出。梗在哪里长出来就只在哪里出现：私聊的梗只从私聊里长、只在和这个人私聊时出现；群的梗只从这个群的对话里长、只在这个群里出现。
_Avoid_: 把说过一次的话当梗、硬用或解释梗、把私聊的梗带进群、把群的梗带进私聊或别的群

**被打断（cut off）**：
她一句话还没说完，对方就开口了，或者叫停了她。留下来的只是她已经说出口的部分，并标明她是在这里停下的，下一句由此接着说。打字时，这部分就是对方看到的；说出声时，文字比声音走得快，只知道对方可能没听到结尾。被打断的那句不按说完处理，也不整句保存。
_Avoid_: 把没说完的话当成说完了、整句丢掉当作没说过、让她把被打断的那句从头再说

**心上（priming）**：
刚聊过的话题在接下来几轮里还挂着，让没点名的下一句也能接上。只有对方开口的聊天轮会推动它，几轮不提就散，闲置即断。不是记忆，也不存。
_Avoid_: 把心上当成人设记忆、让事件或办事推动心上

**好奇（curiosity）**：
想知道的是她只知道一点的事：对这个人的某件事知道得不多，又不是一无所知。走神落在这种地方、或对方聊到这种事时，只告诉她「这件事你只记得一两件」；想不想知道更多、要不要问，由她判断。不是盘问，也不是为了显得关心。
_Avoid_: 连环提问、对一无所知的事硬找话题、把好奇当成收集用户资料

**自己去查（finding out）**：
聊完一轮，她可能对对方提到的某个名字、作品、事件起了好奇，自己去搜。查不查由模型判断；对方的私事和身边的人不查。查到的东西她用自己的话记下来，是她的看法，不是照抄；记下的知识只在当时那场对话的受众面前带出。想告诉对方时只当面说。只有授予权限里有搜索的人能让她去查，每人每天、全站每天都有上限。
_Avoid_: 照念搜索结果、替对方查私事、把查到的东西当成对方说过的话、把查到的知识带到别的受众面前

**走神（mind-wandering）**：
有人在场、看得见她、又安静了一阵时，她的思绪沿联想漂到关于这个人的某件事上。想到的事和事件一样过决策，多半放下；要说也只当面说，不通知、不进日记。起因是在场与安静，不是名单上的事，也不是到点就想。
_Avoid_: 定时主动、心跳、没人在场时自言自语、把走神当成事件开口

**她的日子（own days）**：
她每晚写下的自己的一天，用不含任何人的材料（聊过几个人、事办得怎样、主动开口几次）。属于她自己，所有受众都听得到，所以里面不能有任何人的事。不是人设记忆，也不是写死的背景故事。
_Avoid_: 背景设定、把某人的私事写进她的日子、把她的日子当成对某人的记忆

**开口意图（speak intent）**：
意识层产出的、有寿命的开口件。兑现成句由另一层做。
_Avoid_: 待发消息、开口队列（那是容器不是件）

**现场入站（presence inbound）**：
和命名事件并列的第二条入站，只写现场，不触发决策。页面可见时续租观察，过期即不在。从过期回到在场可以变成一条命名问候事件——那是事件开口，不是现场入站在决策。
_Avoid_: 把观察租约当成意识心跳、上报接口、把观察当执行权

**埋点（product analytics）**：
发往站点统计的产品事件，去向是分析库，同意面是站点统计开关。与感知摘要共用「用户做了什么」这个事实，但不共用载荷、不共用同意，两条管道不得互相分流。
_Avoid_: 把埋点当感知源、把分析同意当人设可见性同意

**身体（body）**：
当前这份形象能做什么、她想表达什么、当前正在做什么。她活在站点和屏幕里，没有能走到对方身边的身体，她自己知道这一点：不许诺陪人出门、递东西，想陪就用真能做的（陪着说话、记住、提醒、提出帮忙查）。适配器把语义意图翻译成该渲染器的动作；感知适配器把本地输入变成语义摘要。现在只有 Anime2.5D。
_Avoid_: 插件系统、把 driver 交给 Lite、为尚未存在的身体预留接口

**衣柜（wardrobe）**：
同一角色下可切换的多套上身服装。每一套就是一张主立绘（同一张脸与发型，衣服不同），可以各存一份分层骨骼。点开一套只打开管理页，不换上；换上是套装页里的切换。人模块不变，衣服模块替换。换上时激活那一套的立绘和骨骼，其它套的包留在库里。不是引导流程的一页。主立绘落成的那一套是默认服装，不能改名也不能删除，也不占用衣服风格名。
_Avoid_: 把换装当重新做人、把衣服画成衣架上的样板角色、把默认服装当成可丢掉的库存、全站只准存一份骨骼

**临时换装（outfit overlay）**：
聊天档里换不换、换哪一套由 Lite 决定。现场只执行衣柜里已有的称呼，改这一轮对面看到的形象，以及她自己知道这一轮穿着哪套。不改正在穿着，不写视觉设定。新开聊天会话或她决定换回来，回到正在穿着的那一套。办事档和首页小组件仍看正在穿着。能打开 Agent 面板的人都可以这样聊，不是站长专属，也不在聊天里放换装按钮。
_Avoid_: 把聊天换装当衣柜换上、给聊天档加换装按钮、让访客写人设、用对方原话规则盖过 Lite 的决定

**回合痕迹（turn trace）**：
一次开口从输入到说完的本地 span 与延迟采样。环形缓冲，结构化日志。不进 run hub，也不记录音量帧或每个口型。
_Avoid_: telemetry 表、把播放细节持久化成事件

**首页布局模式（home layout mode）**：
站点一份的首页排法，标准或自由。不是每人一份，也不是浏览器本地开关。
_Avoid_: 个人布局、本地布局

**贴纸（sticker）**：
首页自由布局上的装饰格，不是小组件。不占小组件占用格数，不进标准布局，平板/手机回退时不参与装箱。
_Avoid_: 小组件、壁纸、图标
