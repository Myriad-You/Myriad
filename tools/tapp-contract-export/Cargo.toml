[package]
name = "myriad-tapp-contract-export"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = ["tapp-contract-schema"]
tapp-contract-schema = []

[dependencies]
schemars = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
