# AGENTS.md

本文件面向在本仓库工作的 AI 代理。人类读者请从 `README.md` 开始。

## 起手动作

1. 读 `CONTEXT.md`。这是术语表，不是规格。本仓库对「权限」有三层区分，混用会直接导致方案失准。
2. 读 `docs/adr/README.md` 拿主题索引，再按需读具体 ADR。ADR 记录的是**已生效的结论**，不是提议。不要重新论证已记录的决策，除非你手上有能推翻它的新事实——那种情况下写一条新 ADR 标注取代关系，不要改旧的。

## 事实纪律

- 结论落到 `文件:行号`。没有位置的断言等于没有断言。
- **外部文档里的符号名不可信。** 设计稿、飞书文档、issue 描述中出现的路径、函数名、字段名，引用前必须在仓库里核实——曾出现某份设计文档通篇引用一个代码中完全不存在的网关路径的情况，照抄会让整份方案落在空气上。
- 无法验证的判断标注 `uncertain`，不要用推论填充事实。浏览器行为、第三方服务策略这类不在平台控制内的东西，说清楚它不在控制内。

## 硬约束

违反下列任一条的改动会被要求返工。

**权限三层不可混用。** 声明权限是应用请求的，批准权限是安装时落库的，授予权限是运行时过滤后实际生效的。只有授予权限决定行为。任何「放宽权限」的改动先说清动哪一层。

**改平台配置默认值前先读 ADR 0001 与 ADR 0004。** 新增配置字段必须同时补上从数据库读回的解析分支，否则一致性测试会红——这条约束的存在就是因为漏过一次，且漏得完全静默。

**密钥不进沙箱。** 宿主密钥与应用凭据只在后端出站路径解析，不出现在任何返回给应用的载荷里，错误信息也算载荷。

**暂存不是销毁。** 代码变更、登录态变更、卸载必须销毁；用户注意力转移只能暂存。判断标准是授权前提有没有消失，不是用户还看不看得见。

**工程脚手架不进主干。** 草稿、临时规格、代理笔记不要提交到 `preview`。

## 输出去向

决策沉淀进 `docs/adr/`，术语沉淀进 `CONTEXT.md`。两者都不放实现细节——实现细节属于代码和 PR 说明。

提交按 Conventional Commit，一个可评审单元一次提交，不要把无关改动攒在一起。
