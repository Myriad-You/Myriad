[package]
name = "myriad-tapp-contract"
version = "0.1.0"
edition = "2021"
publish = false

[features]
tapp-contract-schema = ["dep:schemars"]

[dependencies]
http = "1"
schemars = { version = "1.0", optional = true }
semver = "1.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
url = "2"
