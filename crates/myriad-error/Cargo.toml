[package]
name = "myriad-error"
version = "0.1.0"
edition = "2021"
publish = false
description = "Shared AppError + secret redaction for Myriad backend (and future domain crates)."
authors = ["Myriad Contributors"]
license = "AGPL-3.0-only"

[dependencies]
http = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
