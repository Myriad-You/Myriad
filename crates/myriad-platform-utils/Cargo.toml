[package]
name = "myriad-platform-utils"
version = "0.1.0"
edition = "2021"
publish = false
description = "Platform request spoof helpers (Bilibili / Netease / regional IP-UA) for Myriad fetchers."
authors = ["Myriad Contributors"]
license = "AGPL-3.0-only"

[dependencies]
rand = "0.10"
reqwest = { version = "0.13", default-features = false, features = ["json"] }
serde_json = "1"
