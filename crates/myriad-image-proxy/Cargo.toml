[package]
name = "myriad-image-proxy"
version = "0.1.0"
edition = "2021"
publish = false
description = "Image proxy URL rewrite helpers + shared hotlink host list (profile/export/library)."
authors = ["Myriad Contributors"]

[dependencies]
once_cell = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
url = "2"
urlencoding = "2.1"
