//! Per-addressee affect lives in `myriad_merope::affect`: pure rules, no
//! storage. Re-exported here so the persona's modules read it where they
//! always have.

pub use myriad_merope::affect::*;
